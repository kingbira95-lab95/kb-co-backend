[build]
builder = "nixpacks"

[deploy]
# Railway injects $PORT automatically — we read it in run.py / config.py
startCommand = "python run.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[deploy.healthcheck]
path = "/health"
timeout = 30
interval = 10
